{
  "name": "cup-ssg",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "loadDocs": "rm -r docs && cp -a ../docs .",
    "docusaurus": "npm run loadDocs && docusaurus",
    "start": "npm run loadDocs && docusaurus start",
    "build": "npm run loadDocs && docusaurus build",
    "swizzle": "npm run loadDocs && docusaurus swizzle",
    "deploy": "npm run loadDocs && docusaurus deploy",
    "clear": "npm run loadDocs && docusaurus clear",
    "serve": "npm run loadDocs && docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids"
  },
  "dependencies": {
    "@docusaurus/core": "3.1.1",
    "@docusaurus/preset-classic": "3.1.1",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "yarn": "^1.22.21"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.1.1",
    "@docusaurus/types": "3.1.1"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0"
  }
}
